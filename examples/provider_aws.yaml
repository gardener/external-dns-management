apiVersion: dns.gardener.cloud/v1alpha1
kind: DNSProvider
metadata:
  name: aws
  namespace: default
spec:
  type: AWS
  secretRef:
    name: aws-credentials
  domains:
    include:
    - ringtest.dev.k8s.ondemand.com
