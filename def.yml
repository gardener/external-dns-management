helper: (( merge )) 

settings:
  repokey: ""
  pipeline: external-dns-management
  goVersion: 1.18.2
  branchsets:
    main: (( .helper.main_patch_branch("master") ))
    patch: (( .helper.patch_branch("patch") ))
    dev: (( .helper.dev_branches(["dev"]) ))
