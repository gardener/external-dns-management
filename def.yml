helper: (( merge )) 

settings:
  repokey: ""
  pipeline: external-dns-management
  goVersion: 1.12.8
  branchsets:
    main: (( .helper.main_branch("master") ))
    patch: (( .helper.patch_branch("patch") ))
